<template>
    <!-- $COMMENT$ -->
    <entity-manager :fields="fields" :queryFields="queryFields" apiPrefix="/base/$PATH$" :tree="false" ref="entityManagerRef" :params="params"
    operationsWidth="230px">
    </entity-manager>
</template>

<script setup>
import { ref } from "vue"

const fields = [
$$$loopFields{label: "$FIELD_NAME$", prop: "$FIELD_PROP$", $FIELD_MAX$},$$$
    {label: "添加时间", prop: "createTime", width: "160px", system: true},
    {label: "修改时间", prop: "updateTime", width: "160px", system: true}
]
const queryFields = []
const entityManagerRef = ref()
const params = ref({})

function reload() {
    entityManagerRef.value.reload()
}
</script>