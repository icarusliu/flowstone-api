<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuqi.etl.domain.mapper.EtlJobStateMapper">
    <select id="query" parameterType="com.liuqi.etl.bean.query.EtlJobStateQuery" resultType="com.liuqi.etl.bean.dto.EtlJobStateDTO">
        select
        job_id
        ,execute_date
        ,id
        ,tenant_id
        ,create_user
        ,create_time
        ,update_user
        ,update_time
        ,deleted
        from b_etl_job_state
        where deleted = 0
        <if test="null != id and '' != id">
            and id = #{id}
        </if>
        <if test="null != ids">
            <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </if>
        <if test="null != orderBy and '' != orderBy"> order by ${orderBy}</if>
        <if test="null == orderBy or '' == orderBy"> order by create_time desc
        </if>
    </select>

    <insert id="upsert" parameterType="com.liuqi.etl.bean.dto.EtlJobStateDTO">
        insert into b_etl_job_state(id, job_id, execute_date, state,run_sub)
        values(#{id}, #{jobId}, #{executeDate}, #{state}, #{runSub})
        on duplicate key update id = values(id), state = values(state), run_sub=values(run_sub)
    </insert>
</mapper>